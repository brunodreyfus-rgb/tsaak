export default function TalentCard({t,compact=false}){return <div className="card talent"><img className="avatar" src={t.photo}/><div><div className="kicker">{t.why}</div><h2 style={{margin:'6px 0'}}>{t.name}</h2><p className="muted" style={{marginTop:0}}>{t.title} · {t.location}</p><div>{t.badges.map(b=><span className="chip" key={b}>{b}</span>)}{t.tags.slice(0,compact?1:3).map(b=><span className="chip" key={b}>{b}</span>)}</div>{!compact&&<p className="muted">{t.bio}</p>}</div><div style={{textAlign:'right'}}><div className="score">{t.score}</div><div className="mini muted">Score TSAAK</div><p className="mini">€{t.price} · {t.availability}</p><a className="btn primary" href={`/talent/${t.id}`}>Voir profil</a></div></div>}

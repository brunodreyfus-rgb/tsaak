import { pageStyle, topNav, card, button, ghostButton } from '../components/TsaakUI';
export default function Organisation(){ const color='#FF5C8A'; return <main style={pageStyle('organisation')}>{topNav('organisation')}<section style={card(color)}><div style={{color, fontWeight:900, letterSpacing:2}}>ORGANISATION HUB</div><h1 style={{fontSize:50}}>Book experts for events, panels and thought leadership.</h1><p style={{color:'#AAB3C5'}}>Same tunnel as Media: discovery, shortlist, contact, contract, payment, prestation.</p><div style={{display:'flex', gap:12}}><button style={button(color)}>Create event brief</button><button style={ghostButton(color)}>Search talents</button></div></section></main>}

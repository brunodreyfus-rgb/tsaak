import Layout from '../components/Layout';import Timeline from '../components/Timeline';
export default function Contract(){return <Layout><h1>Contract builder</h1><p className="muted">Template → variables → preview → signature → paiement.</p><div className="card"><Timeline active="Contract"/><div className="grid3" style={{marginTop:18}}><div className="card"><div className="kicker">Template</div>Prestation média</div><div className="card"><div className="kicker">Prix</div>€3,200</div><div className="card"><div className="kicker">Signers</div>Media + Talent</div></div><textarea defaultValue="Intervention live de 12 minutes. Droits image inclus pour diffusion TV + replay 30 jours. NDA: non. Annulation J-1."/><button className="btn primary">Envoyer à signature</button><button className="btn" style={{marginLeft:8}}>Payer</button></div></Layout>}

import { pageStyle, topNav, card, button, ghostButton, StatusRail } from '../components/TsaakUI';
import { talents } from '../data/talents';
export default function Media(){ const color='#00D5FF'; return <main style={pageStyle('media')}>{topNav('media')}<section style={{display:'grid', gridTemplateColumns:'1.25fr .75fr', gap:24}}><div style={card(color)}><div style={{color, fontWeight:900, letterSpacing:2}}>MEDIA COMMAND CENTER</div><h1 style={{fontSize:52, margin:'10px 0'}}>Find the right voice before everyone else.</h1><p style={{color:'#AAB3C5', fontSize:18}}>Search AI, Patchwork, Wanted, Help Me, shortlist and multi-message in one newsroom-grade cockpit.</p><div style={{display:'flex', gap:12, flexWrap:'wrap', marginTop:24}}><a href='/search' style={button(color)}>AI Search</a><a href='/wanted' style={ghostButton(color)}>Create Wanted</a><a href='/help-me' style={ghostButton(color)}>Create Help Me</a><a href='/messaging' style={ghostButton(color)}>Multi messaging</a></div></div><div style={card(color)}><h3>Wanted live status</h3><StatusRail active='PROPOSALS' color={color}/><p style={{color:'#AAB3C5'}}>3 proposals received, 1 external talent creation requested, 2 ready to contact.</p><a href='/proposals' style={button(color)}>Review proposals</a></div></section><h2 style={{marginTop:34}}>Patchwork IA</h2><div style={{display:'grid', gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))', gap:18}}>{talents.slice(0,3).map(t=><div key={t.id} style={card(color)}><img src={t.image} style={{width:'100%', height:210, objectFit:'cover', borderRadius:22}}/><h3>{t.name}</h3><p style={{color}}>{t.role}</p><p>Score <b>{t.score}</b> · {t.price}</p><p style={{color:'#AAB3C5'}}>WHY: {t.why[0]} · {t.why[1]}</p><a href={`/talent/${t.id}`} style={ghostButton(color)}>View profile</a></div>)}</div></main>}
